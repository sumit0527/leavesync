// Auth feature barrel — import from here in routes/app
export { AuthProvider, useAuth, getProfile } from '@/contexts/AuthContext';
export type { AuthContextType } from '@/contexts/auth-context-instance';
